# PDF 文档翻译 MVP 执行与验收

最近更新：2026-08-20

## 目标与边界

`v0.5.1` 已冻结，只做回归维护。PDF 作为后续独立版本推进，当前已完成安全文本层导入、只读检查工作台、统一任务确认和受限 PDF → DOCX 导出核心；用户界面仍未启动真实翻译或导出，因此不提升版本，也不宣称 PDF 翻译已经可用。

首版只支持未加密、带可选择文本层的 `.pdf`，最终目标是让用户预览推断后的阅读顺序，再复用既有文档翻译队列并导出译文版或双语版 DOCX。扫描件 OCR、密码输入、像素级还原、多栏自动重排、公式、手写内容和原 PDF 回写不在 MVP 范围内。

## 第一增量完成情况

- 新增受限 PDF 解析模块和 Tauri 只读选择/检查命令；前端只增加类型安全的调用契约，尚未提供用户入口。
- 输入上限 50 MiB、2,000 页、100,000 个对象；单流解压 16 MiB、单页内容 8 MiB、单段 32 KiB、20,000 段、总文本 24 MiB、序列化结果 48 MiB。
- 非 PDF、空文件、无文本层、空密码加密和有密码加密 PDF 均使用稳定错误码拒绝。解析器 panic 被隔离为安全失败，不把文件路径或原文写入错误。
- 每段保留稳定顺序、页码和 `page:<n>:line:<n>` 位置；图片和批注产生降级警告。所有成功导入都会返回 `reading-order-inferred`，要求 UI 在翻译前展示阅读顺序预览。
- 使用 `lopdf 0.44` 且关闭默认功能，避免引入并行、日期和图片嵌入功能；依赖仍带来 PDF 解压、编码和加密识别相关传递包，必须在发布候选继续复核 EXE/安装包增量。
- 同一 Rust 工具链、同一 `--release` 配置和同一构建目录的 A/B 结果：远端 `master` 基线 EXE 为 26,916,352 字节，当前增量为 28,316,160 字节，增加 1,399,808 字节（5.20%）。当前绝对体积仍低于本机 8 月 13 日旧成品 29,688,832 字节，但 PDF 增量的真实成本不可忽略；后续 UI 增量不得继续新增解析库或重复运行时。

## 真实语料：预期、实际与修正

测试文件只下载到被 Git 忽略的 `.pdf-acceptance/`，仓库仅保存公开来源、SHA-256 和断言，避免提交第三方文档。

| 公开样本 | SHA-256 | 独立提取基线 | 产品实际结果 | 差异与处理 |
|---|---|---:|---:|---|
| [GOV.UK Easy Read guidance](https://assets.publishing.service.gov.uk/government/uploads/system/uploads/attachment_data/file/147646/dh_121927.pdf.pdf) | `4eeaaa58f2dd453528e6b46fd38e09f09354f590829b0110434bcbd9895c0c3c` | 40 页、47,925 字符 | 40 页、213 段、48,358 文本字节、0 个替换字符 | 独立提取器把一处长横线解码成替换字符，产品输出为正常 `–`；保留页级图片忽略警告。 |
| [NSW Know the warnings](https://www.nsw.gov.au/sites/default/files/noindex/2026-03/fact-sheet-know-the-warnings.pdf) | `55c41c41728ddae50c92a80a9963624c4d96895918c462549f1911d163016134` | 1 页、1,604 字符 | 1 页、4 段、1,606 文本字节、0 个替换字符 | 多栏文字在内容流中合并，不能据自动提取证明视觉阅读顺序；强制显示阅读顺序推断警告。 |
| [NSW Know your neighbours](https://www.nsw.gov.au/sites/default/files/noindex/2026-04/fact-sheet-know-your-neighbours.pdf) | `c02f188227d09b5220d8f7fb3d36f5ba15abe8a04793e49fe85b7c33f462919d` | 1 页、1,225 字符 | 1 页、4 段、1,251 文本字节、0 个替换字符 | 可见大标题不是可选择文本，解析结果从正文开始；不伪造标题，记录图片忽略和阅读顺序警告。 |
| [NSW Get Ready Resource Hub](https://www.nsw.gov.au/sites/default/files/noindex/2026-04/fact-sheet-resource-hub.pdf) | `b85626ba3ba32d70ddb359272834ae7d36cdf5b932fcae301d5c660295fe1de3` | 1 页、2,335 字符 | 1 页、4 段、2,355 文本字节、0 个替换字符 | 标题位于正文内容流之后且含布局空格，正文为多栏；真实 UI 验收据此增加行内空白归一化，但不擅自重排段序。 |

独立基线使用 `pdfplumber` 从同一 SHA 文件提取；产品验收调用实际 Rust 解析路径，校验 SHA、页数、最低正文量、关键正文和 Unicode 替换字符。显式运行：

```powershell
cargo test --manifest-path src-tauri/Cargo.toml inspects_real_public_pdf_corpus_against_recorded_expectations -- --ignored --nocapture
```

## 下一步顺序

1. 已完成最小 PDF 工作台：系统选择器、文件统计、降级警告和有界阅读顺序预览；此阶段仍不翻译、不导出。
2. 已将用户确认后的 PDF 段映射到既有文档任务模型：页码与稳定位置进入统一分段，输出模式、目标 DOCX 和不含 API Key 的模型快照可被冻结；导出器接通前不显示开始按钮。
3. 已完成受限 PDF → DOCX 导出核心：只接受与重新检查结果逐段一致的源锚点，支持译文版/双语版、跨页分页、显式 A4 页面参数、重新打开复核和不覆盖原子发布。
4. 下一步把 PDF 任务接入既有 Checkpoint、有界队列、取消和失败段重试，再开放真实模型请求与导出按钮；不得复制第二套调度器。
5. 对真实模型生成的 PDF → DOCX 成品执行 LibreOffice/WPS 逐页验收；PDF 原版式回写继续排除。
6. 审计新增依赖、Release EXE/MSI 增量、冷启动和峰值内存；完成全部质量门禁后再决定版本号和 Release。

每个增量结束必须更新本文件的预期与实际差异，运行全量测试与质量门禁，推送 PR 并等待 CI 通过后再进入下一项。

## 2026-08-20 第一增量审计

- 需求对齐：完成只读文本层导入、安全上限、稳定错误与真实语料；没有加入 UI、模型请求、DOCX 导出、OCR、版本提升或 Release。
- 自动化实际结果：前端 61 个测试文件 / 310 项通过；Rust 155 项单元测试通过，3 项显式语料测试按设计忽略；2 项生命周期、7 项 Native Host 进程和 2 项注册测试通过；严格 Clippy 通过。
- 真实效果：4/4 公开 PDF、共 43 页通过产品解析路径验收，0 个 Unicode 替换字符；空文本层、空密码加密和有密码加密 PDF 均按预期拒绝。可见图形标题缺失、多栏阅读顺序无法自动证明，已通过强制警告和后续 UI 预览要求修正产品承诺。
- 工程门禁：桌面/扩展生产构建通过；最大桌面 chunk 251.43 KiB / 300 KiB，扩展 34.33 KiB / 64 KiB；npm 官方安全审计 0 漏洞；质量报告 PASS。Browserslist 数据约 6 个月未更新，维持既有 P2 维护项。
- 仓库历史格式债未混入本次变更：全仓 `cargo fmt --check` 仍被既有 `db.rs`、`ocr.rs` 阻断，本次新增 Rust 文件已独立 `rustfmt`，`git diff --check` 通过。

## 2026-08-20 第二增量审计

- 需求对齐：文档工作台新增 DOCX / PDF 明确模式切换；PDF 只提供系统选择器、文件/页数/分段/原文字节统计、结构警告和最多 100 段的页码预览。PDF 模式不显示 DOCX 恢复、输出位置、任务确认、模型、翻译、Checkpoint 或导出动作。
- 真实成功路径：在独立应用标识的真实 Tauri 窗口、Windows 系统 PDF 选择器、中文深色主题和 1002×720 窗口中选择 NSW `resource-hub.pdf`。预期为 1 页、4 段、约 2.3 KiB 正文、强制阅读顺序警告且无任务入口；实际逐项一致，源文件未修改。
- 真实差异与修正：首次 UI 预览显示 `Get Ready  Resource Hub` 等布局重复空格，并证明标题实际位于正文内容流之后，而不是完全缺失。实现改为只归一化每行内部的 Unicode 空白，不连接行、不重排段；真实 UI 复测得到 `Get Ready Resource Hub`，重复空格消失，4 份公开语料仍全部通过。
- 真实失败路径：通过同一系统选择器选择被 Git 忽略的非 PDF `.pdf` 文件。预期为稳定中文 `invalid-input` 提示、不显示绝对路径/后端消息并保留上一次有效预览；实际全部一致。
- 主题与纵向适配：新控件复用现有 accent、浅色/深色 token 和可滚动工作台边界；真实深色窗口无原生下拉框或横向溢出。英文/中文目录完整性和用户可见硬编码由 i18n 自动化门禁覆盖；浅色主题继续在全量组件/构建门禁中复核。
- 自动化与容量门禁：前端 62 个测试文件 / 315 项通过；Rust 156 项单元测试通过、3 项显式语料测试按设计忽略，另有 2 项生命周期、7 项 Native Host 进程和 2 项注册测试通过；严格 Clippy、桌面/扩展生产构建和质量报告全部通过。最大桌面 chunk 255.92 KiB / 300 KiB，较第一增量增加 4.49 KiB；扩展仍为 34.33 KiB / 64 KiB；npm 官方安全审计为 0 漏洞。同一 Release 工具链下 EXE 为 28,322,304 字节，较第一增量的 28,316,160 字节只增加 6,144 字节（0.02%），没有新增依赖或重复运行时。默认 npm 镜像不实现安全审计端点，已显式切换官方 registry 后取得上述结果，未将镜像错误误记为产品失败。
- 下一入口：将用户确认后的 PDF inspection 显式映射到既有文档任务模型，继续只复用一套 Checkpoint、队列、取消和失败段重试；在完成真实模型与 DOCX 输出验收前不得把“检查预览”宣传为“PDF 翻译已完成”。

## 2026-08-20 第三增量审计

- 需求对齐：PDF 工作台现在允许选择译文/双语模式与新的 `.docx` 目标，并冻结当前模型、语言、提示词和术语表；PDF 页码、`page:<n>` 部件和稳定源位置进入现有 `DocumentJob`。界面仍不显示开始按钮，不发送正文、不写 Checkpoint、不创建输出，避免在导出闭环缺失时产生付费半成品。
- 真实数据链路：实际 NSW `resource-hub.pdf` 经生产解析得到预期的 1 页、4 段，再映射到共享 PDF Checkpoint 并执行真实保存/重载。实际结果为 4 个分段全部保留 `page:1`、页码 1 和 `page:1:line:<n>` 位置，源文件未改变，预选目标 DOCX 未创建；与预期无差异。
- 队列复用审计：同一 `DocumentTranslationQueue` 已用 PDF 任务契约验证翻译结果、页码和源位置可完整进入 `ready-to-rebuild`，没有新增调度器、并发器、重试器或依赖。真正模型请求仍由下一增量在导出器与取消闭环就绪后开放。
- UI 证据边界：独立标识的真实 Tauri 开发程序成功启动，但 Windows 自动化检测到用户正在操作且验收窗口最小化，按安全规则停止点击。因此本增量不把组件自动化冒充真实点击验收，并继续阻止发布和版本提升；下一增量必须补做系统 PDF/输出选择器、任务确认和“无开始按钮”的真实窗口验收。
- 自动化与容量门禁：前端 62 个测试文件 / 318 项通过；Rust 156 项单元测试通过、4 项显式真实语料测试按设计忽略，另有 11 项进程/注册集成测试通过；真实 PDF→Checkpoint 测试和 4/4 公开 PDF 语料显式通过。严格 Clippy、生产构建、质量报告和 npm 官方 0 漏洞审计通过；最大桌面 chunk 255.99 KiB / 300 KiB，扩展仍为 34.33 KiB / 64 KiB。首次 GitHub CI 暴露全新 Windows runner 冷启动 PowerShell 5.1 安全模块超过既有 15 秒自测超时，本地实际仅约 0.73 秒；已将子进程/测试上限改为有界的 60/70 秒并等待远端复测，不降低任何身份或语料断言。
- 发布判断：当前只完成任务确认，不具备真实模型翻译、崩溃恢复和 DOCX 成品导出，未达到 `0.5.2` 发布条件；版本保持 `0.5.1`，不创建 Release。

## 2026-08-20 第四增量审计

- 需求对齐：新增 PDF 导出计划白名单与 Tauri 命令，复用既有重建注册表、取消边界和不覆盖原子发布；输出支持译文版与双语版，并按 PDF 页码插入分页。前端只建立类型安全的导出计划和命令契约，工作台仍不显示开始/导出按钮，也不发送真实模型请求。
- 安全边界：导出前重新检查原 PDF，逐段复核指纹、数量、ID、顺序、页码、源位置和源文本；限制单段、总译文、XML 与最终包体积，拒绝 XML 控制字符、源文件变化和既有目标。成品落盘前使用真实 DOCX 检查器重新打开，发布仍保持同卷临时文件、同步和原子不覆盖语义。
- 预期与实际差异：首个包含 `&` / `<` 的 round-trip 预期保留实体字符，实际重新检查时字符丢失。根因是既有 DOCX 解析器未处理 Quick XML 的 `GeneralRef` 事件；补齐命名/数字 XML 实体解析后，译文版实际为 `你好 & 世界`、`第二 <页>`，双语版同时保留对应英文原文，测试转绿。首次标准渲染检查又发现生成包只有空 `sectPr`，无法推断页面尺寸；已增加显式 A4 页面尺寸与 1 英寸页边距并固定自动化断言。
- 真实文件结果：公开 `resource-hub.pdf` 经生产解析得到 4 段，分别生成 4 段译文 DOCX 与 8 段双语 DOCX；两份成品均经产品 DOCX 检查器重新打开，源 PDF 前后逐字节不变，输出通过不覆盖原子发布。标准渲染器已能读取页面参数，但本机缺少 LibreOffice；随后启动 Microsoft Word 做真实视觉验收时用户按 Esc 停止了桌面控制，因此本增量不记录任何 Word/WPS/LibreOffice 视觉通过结论。
- 自动化门禁：前端 62 个测试文件 / 320 项通过；Rust 158 项单元测试通过、5 项显式语料/视觉测试按设计忽略，另有 11 项进程/注册集成测试通过；真实 PDF → 双模式 DOCX 测试已显式通过。严格 Clippy、TypeScript、桌面/扩展生产构建、包体审计、官方 npm 0 漏洞审计和质量报告均通过；最大桌面 chunk 255.99 KiB / 300 KiB，扩展 34.33 KiB / 64 KiB，没有新增依赖。
- 发布判断：导出核心已经闭合，但用户运行时尚未接通真实模型、Checkpoint 恢复与导出动作，且双模式成品视觉矩阵未完成；未达到新版本条件，版本保持 `0.5.1`，不更新 README 下载链接、不创建标签或 Release。
