# v0.4.9 Windows 候选构建记录

构建日期：2026-07-30  
构建环境：Windows x64，本地 Tauri release 构建  
配置版本：前端、Cargo、Tauri、`package-lock.json`、`Cargo.lock` 均为 `0.4.9`

## 自动化门禁

- 前端：42 个测试文件、184 项测试通过。
- Rust：70 项单元测试、2 项真实子进程测试通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- 生产构建、300 KiB 包体门槛、`npm audit` 和统一质量报告通过。
- 本地签名公钥与应用内置 Updater 公钥一致。

## 候选资产

| 资产 | 大小（字节） | SHA-256 | Updater 签名 |
| --- | ---: | --- | --- |
| `Long翻译_0.4.9_x64-setup.exe` | 10,104,691 | `E0CE63C487FE4E158B43008694F49F75503D44A97DA79FAE52D17489A704960C` | `.sig` 已生成 |
| `Long翻译_0.4.9_x64_zh-CN.msi` | 22,237,184 | `0489E3839B4672F93D0063BF67E72C1AC9695E400BF51D2426799274E6B61924` | `.sig` 已生成 |

签名文件：

- NSIS `.sig` SHA-256：`CFB799234BBF511BD53861E01229051A6251B502E6C502CB85040E2FC7A56449`
- MSI `.sig` SHA-256：`AF55F74C0E71E80ADDF3C6665036AB7EEE96965DBC16F08CFE569AE12D058AE0`

## Windows 交互烟雾进度

执行时间：2026-07-30 15:36–16:01（Asia/Hong_Kong）

执行环境：Windows 11 家庭中文版 `10.0.26200`、双显示器、主会话 DPI 200%

候选包：上述 NSIS 安装包，操作者为 Codex，安装与运行操作已获用户确认

已通过：

- 覆盖安装成功；安装器能检测并关闭运行中的旧版本，安装后的 `FileVersion` 与 `ProductVersion` 均为 `0.4.9`。
- 安装完成后的首次启动直接显示主窗口，没有仅驻留托盘。
- 主窗口可滚动浏览完整纵向设置内容；双显示器、200% DPI 环境下未观察到内容截断或无法访问的控件。
- 连续启动与关闭到托盘后再次启动均只保留一个 `long-translate.exe` 进程，并恢复同一主窗口。
- 手动检查更新成功返回“已是最新版本”，没有出现 updater 解析错误。
- 更新检查产生 1 条通知后，未读角标显示为 1；打开通知历史后角标归零，点击通知条目后面板关闭且角标未回弹。
- 连续产生 2 条通知后未读角标准确累加为 2；打开通知历史后角标归零，“全部清除”同步清空列表、角标并关闭面板。
- 通用设置中的界面语言下拉框使用应用自定义深色样式，没有退回 Windows 原生下拉框。
- GitHub 正式 v0.4.8 NSIS 安装包（7,497,487 字节，SHA-256 `3C3370723CD51F1BDAB0188BC3160283614BC16F73582F4B3F2E18F96B505DBC`）与 Release 摘要一致；安装后 `FileVersion` 与 `ProductVersion` 均为 `0.4.8`，并复现旧版首次启动仅驻留托盘。
- 从上述正式 v0.4.8 原位升级至候选包成功；升级后版本均为 `0.4.9`、只有一个进程、主窗口自动显示，原有统计数据与 `E:\Long\Long翻译\long-translate.exe --autostart` 注册项保持不变。

发布验收补充证据：

- 通知最后一条删除后关闭面板、通知清空原子性由 `useNotifications` 三项单元测试覆盖，并与真实“全部清除”交互结果一致。
- 托盘左键和“显示设置”菜单映射到同一窗口恢复路径；托盘使用固定 ID `tray`，关闭到托盘、重复启动恢复和单进程已完成真实交互验证，菜单映射由 Rust 单元测试覆盖。
- 开机自启注册项包含 `--autostart`；真实子进程测试确认该参数进入仅托盘模式，手动启动则恢复主窗口。为避免中断当前开发会话，本轮未执行真实注销登录。

## 已知发布边界

- EXE 与 MSI 的 Windows Authenticode 状态为 `NotSigned`；Updater `.sig` 只验证应用内更新包真实性，不消除 SmartScreen 提示。
- 托盘菜单与开机自启的真实注销路径使用代码、注册项、单元测试和真实子进程测试组成替代证据；未执行会中断当前用户会话的注销登录。
- 当前候选包已满足 v0.4.9 发布门槛，可以创建并推送 `v0.4.9` 标签启动正式 Release。
