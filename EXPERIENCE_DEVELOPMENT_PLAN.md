# Long翻译体验打磨开发计划

版本规划：v0.3.8 → v0.3.9 → v0.4.0 → v0.4.5 → v0.4.6 → v0.4.7 → v0.4.8 → v0.4.9 → v0.5.0

制定日期：2026-07-18

最近更新：2026-07-27

## 1. 目标与边界

本周期不扩展新的产品方向，专注把现有的翻译、OCR、生词本、复习和设置体验做完整、稳定、精致。

核心体验目标：

> 用户在任意 Windows 软件中通过一次快捷键，稳定获得可信译文；需要时可以校验、收藏和复习，过程中没有无法理解的状态或多余操作。

本周期明确不做：

- 文档/PDF 翻译
- 浏览器扩展
- 实时语音同传
- 新的翻译供应商插件体系
- macOS、Linux 或移动端

## 2. 开发原则

1. **先主链路，后次级页面**：翻译不可用时，不推进视觉装饰。
2. **状态必须可解释**：每个异步操作都必须有空闲、进行、成功、失败和取消状态。
3. **错误不进入内容**：错误、降级和模型信息不得混入可复制译文。
4. **保持用户控制权**：长操作可以取消，失败操作可以原地重试。
5. **配置渐进展示**：普通用户只看到必要项，高级参数按需展开。
6. **键盘优先但不依赖键盘**：高频操作提供快捷键，同时保留清晰按钮。
7. **每个修复都有回归测试**：纯逻辑优先抽离为可测试模块。

## 3. 版本与实施顺序

### v0.3.8：翻译主链路

#### T1. 统一翻译任务状态

状态模型：

- `idle`：等待输入
- `translating-primary`：主模型翻译中
- `translating-backup`：备用模型接管中
- `success`：翻译完成
- `error`：翻译失败
- `cancelled`：用户取消

验收标准：

- UI 不再通过“译文是否为空”推测任务状态。
- 每次请求拥有独立 ID，旧请求不得覆盖新请求。
- 状态、模型、缓存命中和错误与译文内容分离。

#### T2. 取消、重试与竞态保护

验收标准：

- 浮窗和批量翻译均可取消正在进行的请求。
- 新翻译开始时自动取消旧翻译。
- 取消后停止追加内容，不保存不完整历史。
- 重试复用原始文本，但创建新请求。

#### T3. 主备模型结果隔离

验收标准：

- 主模型失败后，备用模型译文从空白结果开始。
- 主模型的部分结果不与备用结果混合。
- UI 明确显示当前使用的模型和故障切换状态。
- 技术错误不写入译文正文。

#### T4. 浮窗信息与操作层级

验收标准：

- 固定展示复制、收藏、朗读、回译和重试/取消操作。
- 加载、失败、缓存命中和备用模型状态有清晰文案。
- 禁用不可执行操作，并提供可理解的原因。
- 翻译完成后才允许写入历史。

#### T5. API 配置向导与连通测试

验收标准：

- 提供 DeepSeek、OpenAI、自定义兼容接口预设。
- 普通配置只展示服务、API Key、模型；Base URL 和 Prompt 归入高级设置。
- 保存前可测试连接。
- 区分认证失败、模型不存在、限流、超时和网络错误。
- 不在日志或错误信息中暴露 API Key。

#### v0.3.8 发布门槛

- 翻译任务逻辑具有自动化测试。
- 前端构建、Rust 测试、Clippy 和 npm audit 全部通过。
- 手工验证快捷键翻译、剪贴板监听、批量翻译、主备切换和回译。

### v0.3.9：OCR 与学习闭环

#### O1. OCR 结果确认

- OCR 后先展示可编辑原文，再翻译。
- 空识别结果提供重新截图，不打开空译文。
- 记住上次 OCR 语言。
- `Esc` 取消，`Enter` 确认。

#### L1. 带上下文收藏

- 收藏单词时保存原始句子、译文、来源类型和时间。
- 生词详情展示收藏时的上下文。
- 重复收藏时允许补充新上下文，不重复创建单词。

#### L2. 全键盘复习

- 空格翻面，数字 `1–4` 评分，左右键切换。
- 评分后自动进入下一张。
- 完成后展示本轮数量和下一次复习时间。

#### v0.3.9 发布门槛

- OCR 取消、空结果和编辑确认通过手工验证。
- 数据库迁移可重复执行且不会丢失旧生词。
- FSRS 评分键盘流程具有自动化测试。

### v0.4.0：设置、性能与视觉一致性

#### S1. 设置分层

- 基础设置：服务、API Key、模型、语言、快捷键、主题、开机启动。
- 高级设置：Base URL、Prompt、备用模型、TTS、WebDAV、缓存与诊断。
- 对未保存修改、保存成功和快捷键冲突提供统一反馈。

#### S2. 统一反馈规范

- Toast 只承载短暂反馈。
- 可恢复错误在内容区提供重试。
- 阻断错误使用明确说明，不直接展示底层异常。
- 空状态说明下一步操作。

#### P1. 性能与细节

- 减少重复读取配置和数据库连接。
- 长列表保持虚拟化/分页，不阻塞主线程。
- 动画遵循 reduced-motion 系统设置。
- 统一按钮禁用态、焦点态、间距和文案。

#### v0.4.0 发布门槛

- 完成一次完整键盘可访问性检查。
- 所有主页面具有统一的加载、空、错误和成功状态。
- 冷启动、首次翻译和缓存命中耗时有可重复测量结果。

## 4. 工程实施约束

- 新的翻译控制逻辑放在 service/hook 层，不继续堆入 `Dashboard.tsx`。
- React 组件只负责展示和用户交互，不拼接协议错误文本。
- Tauri 命令返回结构化数据或结构化错误，不依赖字符串解析。
- 数据库结构变更必须增加 schema version，并提供旧版本迁移。
- 每完成一个任务，更新本文任务状态并运行对应测试。

## 5. 当前进度

- [x] 制定体验打磨范围与顺序
- [x] T1 统一翻译任务状态
- [x] T2 取消、重试与竞态保护
- [x] T3 主备模型结果隔离
- [x] T4 浮窗信息与操作层级
- [x] T5 API 配置向导与连通测试
- [x] O1 OCR 结果确认
- [x] L1 带上下文收藏
- [x] L2 全键盘复习
- [x] S1 设置分层
- [x] S2 统一反馈规范
- [x] P1 性能与视觉细节
- [x] Q1 Windows DPAPI 敏感配置保护与旧数据迁移
- [x] Q2 WebDAV 密码读取修复
- [x] Q3 更新流程拆分与键盘交互测试
- [x] Q4 CI 与动态发布说明
- [x] Q5 翻译缓存上下文隔离、相关术语筛选与格式约束
- [x] Q6 OCR 自适应预处理、二值化回退与语言覆盖
- [x] Q7 TTS 语言路由、语速与缓存一致性
- [x] Q8 WebDAV 连接测试、同步结果摘要、并发冲突保护与隔离端到端测试
- [x] Q9 生词本大数据量性能基线与服务端分页/检索/排序
- [x] Q10 弹窗焦点循环和全页面键盘验收
- [x] Q11 生产路径错误结构化，消除用户输入可触发的 `unwrap` / `expect`
- [x] Q12 翻译格式保持评测集与主备模型质量门槛
- [x] Q13 TTS 语言/地区路由、音色降级原因与音频容器质量夹具
- [x] Q14 OCR 字符错误率基线与 Windows 已安装语言动态发现
- [x] Q15 OCR 生成式 PNG 金标与真实 Windows 引擎 CER 门槛
- [x] Q16 翻译、TTS、OCR 统一质量报告与 CI/Release artifact
- [x] Q17 国际化类型安全、深层键/占位符门禁与用户可见硬编码清理
- [x] Q18 按 Tauri 窗口与复习页签拆分前端首包
- [x] Q19 历史记录类型契约、按需加载与独立页签边界
- [x] Q20 OpenAI 兼容供应商能力、超时与错误映射策略
- [x] Q21 模型与 TTS 配置独立页签及补丁式状态边界
- [x] Q22 外观设置独立页签、按需加载与无障碍选择状态
- [x] Q23 通用设置独立页签及系统副作用编排边界
- [x] Q24 生词本独立页签、分析容错与响应式详情布局
- [x] Q25 术语表类型化命令、竞态隔离与独立编辑视图
- [x] Q26 批量翻译独立视图、按需加载与任务状态边界保持
- [x] Q27 剪贴板监控命令编排、轮询互斥与卸载隔离
- [x] Q28 术语表 Rust 命令模块、稳定排序与错误传播

## 6. 实施记录

### v0.3.8 主链路

- 翻译任务改为显式状态机，并加入请求 ID、取消、重试、缓存状态与主备模型隔离。
- 浮窗和批量翻译统一使用结构化任务结果，错误信息不再混入译文。
- 模型配置加入服务预设、渐进式高级设置与连接测试。

### v0.3.9 OCR 与学习闭环

- OCR 增加可编辑确认、空结果、失败重试、Enter/Shift+Enter/Esc 键盘流程和过期请求隔离。
- 数据库升级到 schema v7；收藏记录可保存多条原句、译文、来源和时间，并参与备份与 WebDAV 合并。
- 复习支持空格翻面、数字 1–4 评分、方向键切换，并显示本轮数量和下次复习时间。

### v0.4.0 设置、反馈与性能

- 基础设置与 TTS、WebDAV、缓存等高级设置分层展示；未保存、保存中、已保存和失败状态可见。
- Toast 去重并限制同时显示数量，错误/状态补充可访问语义；统一键盘焦点和禁用光标。
- 配置读取和保存改为单连接批量事务；翻译、设置页和生词分析不再逐项重复打开数据库。

### v0.4.5 安全与质量收口

- 敏感配置改用 Windows DPAPI 用户级保护；旧明文和旧 `ENC:` 数据在读取时自动迁移。
- 修复 WebDAV 同步把数据库密文作为 Basic Auth 密码发送的问题。
- 更新检查、下载、进度、安装和失败恢复从 Dashboard 抽离为独立 hook 与纯逻辑 service。
- 更新弹窗和主题选择器补齐键盘操作、ARIA 状态与关闭行为测试。
- GitHub Actions 增加完整持续集成门槛，Release 正文从对应版本文档自动读取。

### v0.4.6 翻译、OCR 与 TTS 精度打磨

- 翻译缓存加入供应商、模型、源/目标语言、Prompt 和术语表指纹，避免配置变化后误命中旧译文。
- 术语表只注入与当前原文相关的条目，并通过 XML 边界和格式保持约束降低提示词歧义。
- OCR 增加放大、灰度、对比度和锐化预处理，并在低质量结果时回退二值化识别。
- TTS 增加中、英、日、韩、俄、阿等文字与区域路由，修复语速和音频缓存一致性。
- 数据库升级到 schema v8，迁移时清理缺少上下文指纹的旧翻译缓存。
- v0.4.6 安装包、更新清单和签名已由 GitHub Actions 发布。

### v0.4.7 WebDAV 可靠性与诊断

- WebDAV 设置增加连接测试、延迟显示、中英文结构化错误和最近同步摘要。
- 同步模块从主入口拆出，并覆盖认证失败、URL 校验、远端损坏数据和历史可空字段。
- 使用 ETag 条件写入检测多设备并发变化，冲突时要求重试合并，不静默覆盖云端内容。
- 增加隔离 WebDAV 服务测试，验证远端下载、本地合并、上下文保存和合并快照上传。
- 生词本改为后端分页、检索和排序，每页最多 200 条；建立一万条数据的首屏与搜索基线。
- 更新与 OCR 确认弹窗统一焦点循环、Escape 关闭和关闭后焦点恢复；主导航支持方向键、Home、End 与 Ctrl+1～7。
- 32 个前端可调用命令统一返回应用目录解析错误；启动、托盘、Anki、WebDAV 和 Edge TTS 路径移除生产态 `unwrap` / `expect`。

### v0.4.8 可量化准确率（已完成首阶段）

- 建立固定翻译格式评测集，覆盖术语、数字、URL、占位符、Markdown、XML、行内代码和长上下文。
- 主翻译、备用翻译、对比翻译与缓存命中统一校验格式不变量；无效缓存会重新请求，主模型不合格时自动切换备用模型。
- 主备模型都未保持必要内容时返回结构化 `format-invalid` 错误，且不把无效译文写入缓存。
- 建立中、英、日、韩、俄、阿 TTS 路由夹具，记录配置音色、默认音色和语言不匹配降级原因。
- 增加 MP3、WAV、Ogg、M4A、WebM、AAC 容器检查；HTML/JSON 错误响应在进入音频解码前即被识别。
- 增加 OCR 字符错误率（CER）计算和五类文本基线，覆盖小字号、深色字幕、缩放、多显示器坐标和中英日韩混排场景。
- OCR 语言选择优先枚举 Windows 已安装识别语言，兼容旧短标签；枚举失败时保留静态语言列表。
- 测试运行时确定性生成小字号、深色字幕和缩放 PNG，将图片实际送入 Windows OCR；当前门槛为单场景 CER 不超过 20%。
- CI 与正式发版汇总翻译格式、TTS 路由/音频和 OCR 文本/真机结果，生成带期望值、实测值、阈值与 Git 提交号的 JSON 报告。
- CI 报告保留为 Actions artifact；正式发版除 workflow artifact 外，还会把报告附加到 GitHub Release。
- 中英文目录改为同一类型契约，CI 测试校验深层键、插值占位符和组件标记中的用户可见硬编码；动态错误码和上下文来源通过带回退规则的翻译解析器读取。
- 主窗口、翻译浮窗、OCR 覆盖层和复习页签改为按需加载；生产构建最大入口块由 516.76 kB 降至 238.83 kB，并增加三类 Tauri 窗口及浏览器预览路由回归和 300 KiB 单块 CI 门禁。
- 历史记录页从 Dashboard 拆为独立懒加载组件和类型化命令 service；仅在首次打开页签时读取数据，并独立处理更新事件、失败重试、删除和清空。
- DeepSeek、OpenAI 和自定义兼容接口统一使用类型化能力描述；真实翻译与连接测试共用端点规范、请求/探测超时及认证、模型不存在、限流和服务端错误映射。
- 模型与 TTS 配置视图从 Dashboard 拆为独立懒加载页签；仅通过翻译/TTS 值对象和类型化补丁回调连接 `useSettings`，页签内部只保留展开状态。
- 外观设置从 Dashboard 拆为独立懒加载页签；主题、主题色和界面缩放通过单一值对象与类型化补丁回调连接 `useSettings`，颜色与缩放控件补齐中英文无障碍名称和选择状态。
- 通用设置从 Dashboard 拆为独立懒加载页签；组件只管理高级区域展开状态并发出类型化设置、快捷键、WebDAV 和维护操作事件，自启动、快捷键注册、同步、备份、缓存与诊断等 Tauri 副作用继续由 Dashboard 编排。
- 生词本列表、筛选、分页、添加和详情视图从 Dashboard 拆为独立懒加载页签；导出、朗读和删除等副作用通过显式回调连接既有 hook/编排层，窄窗口改为上下布局。分析 JSON 使用容错解析器，旧数据或损坏数据不再导致整个页签崩溃，而是进入可重试失败状态。
- 术语表命令收敛到类型化 service，加载和写入编排由独立 hook 管理；请求编号阻止旧读取覆盖新数据，同步写入锁阻止快速连点重复提交。编辑器按需加载并独立处理草稿、校验、失败重试、键盘保存/取消、主题与窄窗口布局。
- 批量翻译输入、输出、双模型对比、回译、取消、错误重试和术语表组合视图从 Dashboard 拆为独立懒加载组件；既有 `useBatchTranslation` 仍由 Dashboard 持有，切换页签不会中断进行中的任务。对比结果在窄窗口改为纵向布局，输入区补充可访问名称，术语表只在首次打开批量翻译页签后加载。
- 剪贴板读取、文本去重、浮窗触发和定时器清理从 Dashboard 拆为独立 hook；同一时间只允许一次系统读取，停用或卸载后不会继续转发迟到结果。Dashboard 不再直接持有剪贴板轮询引用。
- 术语表四个 Tauri 命令从 Rust 主入口迁入独立模块，前端命令名称和参数保持不变；新增记录返回数据库真实创建时间，同一秒写入通过 ID 稳定倒序，列表不再静默丢弃无法读取的行。独立数据库测试覆盖新增、列表、更新和删除。

### 自动化验收

- `npm test`：31 个测试文件、136 项测试通过。
- `npm run build`：TypeScript 与 Vite 生产构建通过。
- `npm run audit:bundle`：最大生产 JavaScript 块不超过 300 KiB。
- `npm run quality:report -- --require-runtime`：统一质量报告生成并通过全部阈值。
- `npm audit --audit-level=high`：0 个漏洞。
- `cargo test`：41 项测试通过，覆盖 WebDAV 可靠性、生词本性能基线、翻译历史分页与缓存隔离、术语表 CRUD、FSRS 调度/事务提交/统计边界、脱敏诊断边界、Windows OCR 真实 PNG 识别、TTS 缓存、备份数据保护，以及 Anki collection/APKG 结构与字段安全。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `git diff --check`：通过。

## 7. 2026-07-27 代码与交付审计

完整的当前状态、风险分级、v0.4.9 退出门槛和 v0.5.0 浏览器扩展技术路线见 [`docs/DEVELOPMENT_AUDIT_2026-07-27.md`](docs/DEVELOPMENT_AUDIT_2026-07-27.md)。

| 维度 | 当前结论 | 主要证据或风险 |
| --- | --- | --- |
| 版本与交付 | v0.4.8 已完成正式签名发布 | GitHub Release 已包含 EXE、MSI、更新签名、`latest.json` 和质量报告；Windows Authenticode 尚未配置 |
| 翻译质量 | 已有固定格式保持样本集与运行时质量门槛 | 目前衡量结构保持，仍需真实双语语义金标与版本间准确率报告 |
| OCR | 已有自适应预处理、CER 文本基线、生成式 PNG 真机识别门槛和 Windows 已安装语言动态发现 | 仍缺来自真实应用的截图资产、中日韩语言包可控门槛和跨机器 CER 趋势报告 |
| TTS | 已有多语种路由、降级原因、语速、缓存与常见音频容器夹具 | Edge TTS 依赖非正式消费者接口，真实发音质量和远端可用性仍需周期性验证 |
| 同步与数据 | DPAPI、连接测试、结构化摘要、并发保护和隔离端到端验证已完成 | 后续需验证更多真实 WebDAV 服务兼容性 |
| 性能 | 历史和生词本均已后端分页；生词本具备一万条首屏/搜索基线；窗口、复习、历史、模型、外观、通用设置、生词本、批量翻译和术语表视图按需加载后最大生产块为 234.55 KiB，批量翻译视图自身为 11.26 kB | 后续继续按真实启动指标审计共享依赖与大型页签渲染成本 |
| 可访问性 | 更新与 OCR 弹窗具备焦点循环、Escape 关闭和焦点恢复；主导航支持方向键、Home、End 与 Ctrl+1～7 | 后续新增模态界面需继续复用统一焦点约束 |
| 可维护性 | WebDAV、词库查询、诊断、OCR、翻译历史/记忆、复习调度、TTS/音频缓存、加密备份、Anki 导出和术语表已有独立 Rust 模块与测试边界；设置持久化、批量翻译状态与视图、生词本、术语表、历史、模型/TTS、外观、通用设置与剪贴板监控已有独立前端边界 | `Dashboard.tsx` 约 1058 行，`api.ts` 约 852 行；`src-tauri/src/lib.rs` 已由约 1055 行降至约 995 行，直接声明命令由 29 个降至 25 个，下一步优先迁移配置命令，再审计窗口/剪贴板系统命令 |
| 国际化 | 中英文目录使用同一类型契约，并具备深层键、占位符及组件用户可见硬编码门禁 | 后续新增语言时仍需补充母语审校与伪本地化布局回归 |

结论：当前项目已经完成“功能闭环”和首阶段“质量工程化”，下一阶段应完成 v0.4.9 架构与进程级回归收口。浏览器扩展在稳定、版本化的桌面通信协议完成后进入 v0.5.0，不同时扩展文档翻译和实时语音。

## 8. 后续实施顺序与退出门槛

### v0.4.7：可靠性与可诊断性（已完成）

1. WebDAV 增加连接测试、错误分类、冲突/合并摘要和最近同步结果。
2. 提供脱敏诊断导出，不包含 API Key、WebDAV 密码、原文或译文。（已完成：仅导出版本、配置状态、数据量和明确的隐私排除清单）
3. 生词本改为后端分页、检索和排序；以 1 万条数据建立启动、检索和翻页基线。
4. 将用户输入和网络响应可触发的 `unwrap` / `expect` 改为结构化错误。
5. 完成更新弹窗焦点循环和主页面键盘验收。

退出门槛：

- WebDAV 成功、认证失败、超时、远端冲突和离线场景都有自动化或隔离集成测试。
- 1 万条生词下首次可交互、检索和翻页均有固定测试方法，且 UI 不长时间阻塞。
- 诊断包经测试确认不含敏感字段和翻译正文。

### v0.4.8：可量化的准确率（首阶段已完成）

1. 建立 OCR 金标截图集，覆盖小字号、深色字幕、缩放、多显示器和中英日韩混排，记录字符错误率。（CER、文本基线和三类生成式 PNG 真机识别门槛已完成；真实应用截图与跨语种图片仍待补充）
2. 建立翻译评测集，覆盖术语、数字、URL、占位符、Markdown/XML 和长句上下文，比较主备模型与缓存路径。（首批格式保持样本与运行时门槛已完成）
3. 建立 TTS 语言/地区/发音夹具，记录选择音色、输出可播放性和降级原因。（语言路由、降级原因与音频容器首批夹具已完成）
4. OCR 语言列表优先读取 Windows 已安装语言，并保留静态回退。（已完成）

退出门槛：

- 每次发布都能生成 OCR、翻译格式保持和 TTS 路由的对比报告。
- 质量下降有明确阈值并阻断发布，而不是只依赖人工听感或观感。

### v0.4.9：架构收口

1. Rust 按 WebDAV、词库/复习、历史、TTS、备份和 Anki 拆分模块，统一命令错误类型。（主要领域已完成拆分；Anki APKG 现具备结构、字段安全和空集合边界测试）
2. 前端按设置、生词本、批量翻译和更新流程拆分组件与 hook。（更新、批量翻译、生词本编排与视图、术语表 service/hook/编辑视图、设置持久化、历史记录、模型/TTS、外观及通用设置边界已完成；主窗口/浮窗/OCR、复习页、历史页、模型配置页、外观页、通用设置页、生词本页与术语表视图已按需加载）
3. 在不扩展供应商数量的前提下，提炼现有 OpenAI 兼容接口的能力描述、超时和错误映射。（已完成：三类配置共用类型化策略，翻译请求与连接探测不再重复分类）

退出门槛：

- `Dashboard.tsx` 和 `lib.rs` 不再承担跨领域编排细节。
- 核心业务模块可以独立测试，新增错误路径不再依赖字符串匹配。

### v0.5.0：浏览器扩展

v0.4.7–v0.4.9 的可靠性、质量和架构门槛完成后，正式进入浏览器扩展方向。首个版本聚焦网页划词、上下文翻译、术语表复用与一键收藏到桌面端生词本，不同时扩展文档翻译或实时语音。

进入门槛：

- 桌面端提供稳定、版本化的本地通信协议，不向扩展暴露 API Key、WebDAV 密码或数据库文件。
- 明确扩展与桌面端的配对、授权、断线恢复和版本兼容策略。
- Chrome / Edge 共用一套 Manifest V3 代码，先验证核心阅读闭环，再评估 Firefox。
- 网页原文、译文和浏览记录默认不进入诊断信息或云同步。
- 桌面通信优先采用受控 Native Messaging Host；扩展只获得版本化能力，不获得 API Key、WebDAV 密码或数据库访问能力。
